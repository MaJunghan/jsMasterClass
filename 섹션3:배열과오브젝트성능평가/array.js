// array 배열 : 정렬된 데이터

// 정렬이 되어 있는 것은 경우에 따라서 연산 시간이 더 걸릴수 있다.

// 배열은 대부분 정렬되어 있는 데이터를 위해서 사용한다.

// insert O(n) <==> push O(1)
// remove O(n) <==> pop O(1)
// search O(n)
// access O(1)

// 정리 : 배열은 앞에추가하는것보다 뒤에 추가하는게 빠르다.

// sort - O(n * log N) : O(n)보다 느림
